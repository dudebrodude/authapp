<strong>Following</strong> (<%= current_user.followed_users.count %> total)
      <% if @users.any? %>
        <div class="user_avatars">
          <% @users.limit(5).each do |user| %>
            <div class="buddy-list"><%= link_to gravatar_for(user, size: 30), user %> <%= link_to user.name, user %>
            </div>
     
          <% end %>

        </div>
                  <%= link_to "View All", following_user_path(current_user), class:"btn btn-primary" %>
        <% else %>
        <p>
        You aren't following anyone yet! 
      </p>
      <% end %>
